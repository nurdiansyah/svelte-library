---
message: |
  hygen {bold component new} --name {bold NAME} {bold [--project]} {bold [--category]}

    Generates a svelte component with scss.

       NAME           The component name in kebab-case ({bold required}).
       --project     Generate a stateful component (bold required).
       --category   Generate a functional component (bold required).

---
